//Import the application logic module/etc, if done with creating that then import the UI module
import {projects, loadStorage, saveToStorage, createProject, deleteProject, Checklist, Project, Todo} from './todoApp.js'

