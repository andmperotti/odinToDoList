import { todoApp } from './todoApp.js'

export const UIController =(function(){
    //Generate site layout elements
        //sidebar with projects in a list (could sort these later with uncompleted task count vs due dates/etc), including a button where a new project can be made
        //main content area where projects are opened up and todos can be seen and made. If no projects exist maybe generate an example project or explanation text that tells a user how to create a new project

    //I forgot I have the html template file so I can create the starting elements there instead

    //generate projects that are saved (function)

    //generate project when user creates it (same function as above just called at different time (listener on a button))

    //function that adds a checklist or note item into a todo and then redraws or adds that checklist or note item as an element

    
})()